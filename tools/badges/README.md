## Badge generation (local SVGs)

This folder generates **local** README badges (no `shields.io` URLs) using [`badge-maker`](https://www.npmjs.com/package/badge-maker).

### Generate

```bash
cd tools/badges
npm install
npm run generate
```

Badges are written to `docs/assets/badges/`.

